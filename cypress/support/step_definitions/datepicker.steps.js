import { When, Then } from '@badeball/cypress-cucumber-preprocessor';

// When('I select Sales Month from {string} to {string}', (from, to) => {
// 	cy.get(`input[placeholder='Start month']`)
// 		.clear({ force: true })
// 		.type(from + '{enter}');
// 	cy.get(`input[placeholder='End month']`)
// 		.clear({ force: true })
// 		.type(to + '{enter}');
// });
